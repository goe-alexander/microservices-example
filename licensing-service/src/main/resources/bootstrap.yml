spring:
  application:
    name: licensingservice
  profiles:
    active: default
  cloud:
    config:
      enabled: true
  jpa:
    hibernate:
      ddl-auuto: update
    show-sql:
      - false

encrypt:
  key: "IMSYMMETRICNOW"